// src/pages/Index.tsx
import React from 'react';
import { Moon, Star, Play, Mail, Heart, Clock } from 'lucide-react';
import { Link } from 'react-router-dom';
import storiesData from '../data/stories.json';
import { EmailSignup } from '../components/EmailSignup';

const Index: React.FC = () => {
  // Determine featured story
  const featuredStory = storiesData.stories && storiesData.stories.length > 0
    ? storiesData.stories[storiesData.stories.length - 1]
    : {
        id: 'coming-soon',
        title: 'Coming Soon...',
        excerpt: 'Your first magical story is on its way!',
        thumbnailUrl: '/placeholder.svg',
        duration: '0:00',
        youtubeId: undefined,
        audioUrl: undefined,
        publishedAt: '',
        tags: []
      };

  // Get recent stories for preview
  const recentStories = storiesData.stories ? storiesData.stories.slice(-3) : [];

  return (
    <div className="min-h-screen bg-gradient-to-b from-purple-50 to-blue-50">
      {/* Hero Section */}
      <div className="relative overflow-hidden bg-gradient-to-r from-purple-600 via-blue-600 to-indigo-600 text-white">
        <div className="absolute inset-0 bg-black opacity-20" />
        <div className="relative max-w-6xl mx-auto px-4 py-20 text-center">
          <div className="flex justify-center mb-6">
            <div className="bg-white bg-opacity-20 p-4 rounded-full">
              <Moon className="w-12 h-12" />
            </div>
          </div>
          <h1 className="text-5xl md:text-6xl font-bold mb-6">Welcome to Snoozies</h1>
          <p className="text-xl md:text-2xl mb-8 max-w-3xl mx-auto opacity-90">
            Magical bedtime stories that help your little ones drift off to dreamland. New calming adventures delivered weekly.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Link
              to={`/story/${featuredStory.id}`}
              className="bg-white text-purple-600 px-8 py-4 rounded-full font-semibold text-lg hover:bg-opacity-90 transition-colors flex items-center gap-2 justify-center"
            >
              <Play className="w-5 h-5" /> Listen to Latest Story
            </Link>
            <Link
              to="/subscribe"
              className="border-2 border-white text-white px-8 py-4 rounded-full font-semibold text-lg hover:bg-white hover:text-purple-600 transition-colors flex items-center gap-2 justify-center"
            >
              <Mail className="w-5 h-5" /> Get Weekly Stories
            </Link>
          </div>
        </div>
      </div>

      {/* Featured Story */}
      <div className="max-w-6xl mx-auto px-4 py-16">
        <div className="text-center mb-12">
          <h2 className="text-4xl font-bold text-gray-900 mb-4">
            {storiesData.stories && storiesData.stories.length > 0 ? "Tonight's Featured Story" : "Your First Story Coming Soon"}
          </h2>
          <p className="text-xl text-gray-600">
            {storiesData.stories && storiesData.stories.length > 0
              ? "Fresh from our storytelling garden 🌙"
              : "We're preparing something magical for you! 🌟"}
          </p>
        </div>
        <div className="bg-white rounded-2xl shadow-xl overflow-hidden max-w-4xl mx-auto">
          <div className="md:flex">
            <div className="md:w-1/2">
              <div className="relative">
                <img
                  src={featuredStory.thumbnailUrl}
                  alt={featuredStory.title}
                  className="w-full h-64 md:h-full object-cover"
                  onError={(e) => (e.currentTarget.src = '/placeholder.svg')}
                />
                <div className="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center">
                  <button className="bg-white bg-opacity-90 p-4 rounded-full hover:bg-opacity-100 transition-colors">
                    <Play className="w-8 h-8 text-purple-600" />
                  </button>
                </div>
                <div className="absolute bottom-4 right-4 bg-black bg-opacity-70 text-white px-3 py-1 rounded-full text-sm flex items-center gap-1">
                  <Clock className="w-3 h-3" /> {featuredStory.duration}
                </div>
              </div>
            </div>
            <div className="md:w-1/2 p-8">
              <h3 className="text-2xl font-bold text-gray-900 mb-4">{featuredStory.title}</h3>
              <p className="text-gray-600 mb-6 leading-relaxed">{featuredStory.excerpt}</p>
              <div className="flex flex-wrap gap-2 mb-6">
                {featuredStory.tags?.map((tag, idx) => (
                  <span key={idx} className="bg-purple-100 text-purple-700 px-3 py-1 rounded-full text-sm font-medium">
                    {tag}
                  </span>
                ))}
              </div>
              <div className="flex gap-3">
                <Link
                  to={`/story/${featuredStory.id}`}
                  className="bg-purple-600 text-white px-6 py-3 rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                >
                  <Play className="w-4 h-4" /> Listen Now
                </Link>
                <button className="border border-gray-300 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-50 transition-colors flex items-center gap-2">
                  <Heart className="w-4 h-4" /> Save
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Newsletter Signup */}
      <div className="py-16">
        <EmailSignup />
      </div>

      {/* Recent Stories Preview */}
      <div className="max-w-6xl mx-auto px-4 py-16">
        <div className="text-center mb-12">
          <h2 className="text-3xl font-bold text-gray-900 mb-4">
            {recentStories.length > 0 ? "More Sweet Dreams" : "Stories Coming Soon"}
          </h2>
          <p className="text-xl text-gray-600">
            {recentStories.length > 0
              ? "Explore our collection of calming bedtime stories"
              : "More magical stories will appear here as they're created"}
          </p>
        </div>
        <div className="grid md:grid-cols-3 gap-6 mb-8">
          {recentStories.length > 0 ? (
            recentStories.map((story) => (
              <div key={story.id}>
                {/* Consider replacing this with <StoryCard /> as a next step */}
                <img
                  src={story.thumbnailUrl}
                  alt={story.title}
                  className="w-full h-48 object-cover rounded-lg"
                  onError={(e) => (e.currentTarget.src = '/placeholder.svg')}
                />
                <h3 className="text-xl font-bold text-gray-900 mt-4">{story.title}</h3>
              </div>
            ))
          ) : (
            [1, 2, 3].map((i) => (
              <div key={i} className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
                <div className="h-48 bg-gradient-to-br from-purple-200 to-blue-200 flex items-center justify-center">
                  <Star className="w-12 h-12 text-purple-400" />
                </div>
                <div className="p-6">
                  <h3 className="text-xl font-bold text-gray-900 mb-2">Coming Soon...</h3>
                  <p className="text-gray-600 mb-4">More magical stories are being crafted just for you!</p>
                </div>
              </div>
            ))
          )}
        </div>
      </div>
    </div>
  );
};

export default Index;
